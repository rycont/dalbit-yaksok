# 1. 메소드 이름을 변수명으로 사용하기 (섀도잉 테스트)
데이터 = [1, 2, 1, 3, 2, 1] . 빈도
키들 = 데이터 . 키들
"--- 키들 목록 ---" 보여주기
키들 보여주기

# 2. 슬래시(/) 포함 조사 기반 호출 테스트
약속, (물건)을/를 보여주기
    "물건: " + 물건 보여주기

("사과")를 보여주기
("포도")을 보여주기

# 3. MBPP 40 (중첩 목록 빈도) 재도전
약속, (중첩데이터) 빈도세기
    결과표 = {}
    반복 중첩데이터 의 x 마다
        # 하위 목록을 문자열로 변환하여 키로 사용
        글자키 = x . 합치기 (",")
        만약 결과표 . 안에 (글자키) 가 있는지 이면
            현재값 = 결과표[글자키]
            결과표[글자키] = 현재값 + 1
        아니면
            결과표[글자키] = 1
    결과표 반환하기

"--- 중첩 목록 빈도 ---" 보여주기
( [[1, 2], [1, 3], [1, 2]] 빈도세기 ) 보여주기
