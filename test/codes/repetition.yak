횟수 = 0

반복
	횟수 보여주기
	횟수 = 횟수 + 1

	만약 횟수 == 10 이면
		반복 그만

5번 반복
	"A" 보여주기

반복 3번
	"B" 보여주기

목록 = ["X", "Y", "Z"]
반복 목록 의 항목 마다
	항목 보여주기

목록 = ["M", "N"]
목록 의 항목 마다 반복하기
	항목 보여주기

목록 = ["A", "B"]
2번 반복
	반복 목록 의 항목 마다
		항목 보여주기

숫자 = 0
10번 반복
	숫자 = 숫자 + 1
	숫자 보여주기
	만약 숫자 == 3 이면
		반복 그만
